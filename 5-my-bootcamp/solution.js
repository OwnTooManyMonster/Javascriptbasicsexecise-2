let varA = ["Yvonne", "Jörg", "David"];
for (let i = 0; i < varA.length; i++) {
  //initiation    condition     updates
  console.log(`#${i + 1} ${varA[i]}`);
} //show from i to condition  i elements of array
